apiVersion: apps/v1
kind: Deployment
metadata:
  name: svp-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: svp-api
  template:
    metadata:
      labels:
        app: svp-api
    spec:
      containers:
        - name: api
          image: ghcr.io/guardianninja/svp-api:latest
          ports:
            - containerPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: svp-api
spec:
  selector:
    app: svp-api
  ports:
    - port: 80
      targetPort: 8000

