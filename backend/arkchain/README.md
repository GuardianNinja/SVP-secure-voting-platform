# ArkChain service

ArkChain runs as a separate service under `backend/arkchain/` with:

- block and entry models
- append and read APIs
- consensus scaffolding (Raft)
- storage and crypto boundaries

